package sneakpeek

class TestCalculator {

    fun plus(first: Long, second: Long): Long {
        return first + second
    }

    fun minus(first: Long, second: Long): Long {
        return first - second
    }
}